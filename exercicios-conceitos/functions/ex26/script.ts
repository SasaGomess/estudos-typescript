// function normalizar(valor: string): string;
// function normalizar(valor: Array<string>): Array<string>;
// function normalizar(valor: string | Array<string>): string | Array<string>{
//     if(typeof valor === 'string'){
//         return valor.trim().toLowerCase();
//     }
//     else return valor.map(item => item.trim().toLowerCase());
// }

// console.log(normalizar(["Banana ", " UVa   ", "Abacaxi  "]));
// console.log(normalizar(" Produto"));

// function $(seletor: "a"): HTMLElement | null
// function $(seletor: "video"): HTMLVideoElement | null
// function $(selector: string): Element | null
// function $(seletor:string): Element | null {
//     return document.querySelector(seletor);
// }

// $('a')?.click();

// $('video')?.volume;

// $('.item')?.getAttribute;

